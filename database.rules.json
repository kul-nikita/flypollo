{
  "rules": {
    ".read": false,
    ".write": false,
    "session": {
      "live": {
        ".read": true,
        ".write": "(!data.exists() || (data.hasChild('status') && ((data.child('status').val() == 'idle' && (newData.child('status').val() == 'idle' || newData.child('status').val() == 'live')) || (data.child('status').val() == 'live' && (newData.child('status').val() == 'live' || newData.child('status').val() == 'ended' || newData.child('status').val() == 'idle')) || (data.child('status').val() == 'ended' && newData.child('status').val() == 'idle')))) && newData.hasChildren(['status', 'questionIndex']) && (newData.child('status').val() == 'idle' || newData.child('status').val() == 'live' || newData.child('status').val() == 'ended') && newData.child('questionIndex').isNumber() && newData.child('questionIndex').val() >= 0 && newData.child('questionIndex').val() % 1 == 0 && newData.childrenCount() <= 6 && (!newData.hasChild('sessionId') || newData.child('sessionId').isString()) && (!newData.hasChild('sessionDate') || newData.child('sessionDate').isString()) && (!newData.hasChild('roomCode') || newData.child('roomCode').isString()) && (!newData.hasChild('questionShownAt') || newData.child('questionShownAt').isNumber())"
      }
    },
    "sessions": {
      "$sessionId": {
        "live": {
          ".read": true,
          ".write": "(!data.exists() || (data.hasChild('status') && ((data.child('status').val() == 'idle' && (newData.child('status').val() == 'idle' || newData.child('status').val() == 'live')) || (data.child('status').val() == 'live' && (newData.child('status').val() == 'live' || newData.child('status').val() == 'ended' || newData.child('status').val() == 'idle')) || (data.child('status').val() == 'ended' && newData.child('status').val() == 'idle')))) && newData.hasChildren(['status', 'questionIndex']) && (newData.child('status').val() == 'idle' || newData.child('status').val() == 'live' || newData.child('status').val() == 'ended') && newData.child('questionIndex').isNumber() && newData.child('questionIndex').val() >= 0 && newData.child('questionIndex').val() % 1 == 0 && newData.childrenCount() <= 6 && (!newData.hasChild('sessionId') || (newData.child('sessionId').isString() && newData.child('sessionId').val() == $sessionId)) && (!newData.hasChild('sessionDate') || newData.child('sessionDate').isString()) && (!newData.hasChild('roomCode') || newData.child('roomCode').isString()) && (!newData.hasChild('questionShownAt') || newData.child('questionShownAt').isNumber())"
        }
      }
    }
  }
}
